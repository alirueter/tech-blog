<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  {{!-- ADD STYLESHEET - USE BOOTSTRAP OR MATERIALIZE?? --}}
  <!-- CSS only -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
</head>

<body>
  <div class="wrapper">
    <header>
      <nav class="navbar navbar-dark bg-dark">
        <h1>
          <a href="/" class="navbar-brand">The Tech Blog</a>
        </h1>
        {{#if loggedIn}}
        <a href="/dashboard" class="nav-link text-white">Dashboard</a>
        <button id="logout" class="btn btn-info">Logout</button>
        {{else}}
        <button id="login" class="btn btn-info"><a href="/login" class="text-white">Login</a></button>
        {{/if}}
      </nav>
    </header>
    <main class="ml-3">
      {{{ body }}}
    </main>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>